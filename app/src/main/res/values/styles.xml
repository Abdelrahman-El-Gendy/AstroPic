<resources>
    <style name="ToolbarTitleTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:fontFamily">@font/parkinsans</item> <!-- Replace with your font -->
        <item name="android:textColor">@android:color/black</item> <!-- Optional: Set the text color -->
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item> <!-- Optional: Set the text size -->
    </style>
</resources>
